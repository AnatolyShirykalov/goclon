<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Classic online parser</title>
     <meta name="csrf-token" content="{{.csrf_token}}" />
  </head>
  <body>
    {{yield}}
  </body>
</html>
